import React from 'react';
import {Text, View} from 'react-native';

const MapNotificateView = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style ={{fontSize: 30}}>Notificate Screen!</Text>
    </View>
  );
};
export default MapNotificateView;
