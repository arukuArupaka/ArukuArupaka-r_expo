import React from 'react';
import {Text, View} from 'react-native';

const MapFavoriteView = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style ={{fontSize: 30}}>Favorite Screen!</Text>
    </View>
  );
};
export default MapFavoriteView;
